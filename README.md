# Plaimanas Landing Page (Frontend Assessment)

โปรเจกต์หน้าเว็บไซต์ E-Commerce Responsive ที่พัฒนาด้วย HTML, CSS และ Vanilla JavaScript โดยมุ่งเน้นที่การรีดประสิทธิภาพ (Performance Optimization) การจัดการทรัพยากร (Asset Management) และประสบการณ์ผู้ใช้ (Smooth UI/UX) ระดับ High-end

## ⚡️ Tech Stack & Architecture
- **Core:** HTML5, CSS3 (Custom Properties, Flexbox, CSS Grid), Vanilla JavaScript
- **Styling Methodology:** BEM (Block Element Modifier) Naming Convention & Component-based CSS
- **Media Delivery:** Cloudinary CDN สำหรับบีบอัด Video และ Image แบบ Real-time

## 🚀 Key Highlights & Optimizations

**1. Performance & Media Optimization**
- **CDN Transformation:** ย้ายไฟล์ Video/Image ขนาดใหญ่ขึ้น Cloudinary และใช้ Parameter (`q_auto, f_auto, ac_none`) เพื่อให้ AI แปลงฟอร์แมตและลดขนาดไฟล์ลงกว่า 80% โดยอัตโนมัติ
- **Smart Loading:** แบ่งการโหลด Media อย่างมีกลยุทธ์ ทำ `Preload` สำหรับภาพ Hero Section เพื่อเร่งค่า LCP (Largest Contentful Paint) และทำ `Lazy Loading` (`preload="none"`, `loading="lazy"`) สำหรับส่วนที่อยู่ต่ำกว่าหน้าจอแรก
- **Non-Blocking CSS:** ยกเลิกการใช้ `@import` เพื่อแก้ปัญหา Waterfall Effect และจัดเรียง Core CSS ให้เบราว์เซอร์ดาวน์โหลดแบบขนาน (Parallel Loading)

**2. UI/UX & Fluid Animations**
- **Hardware Acceleration:** เขียนแอนิเมชัน (เช่น โลโก้ Sticky และ Fade-up) โดยใช้ `transform` และ `opacity` ร่วมกับ `will-change` เพื่อลดภาระ CPU และให้การ์ดจอ (GPU) ทำงานแทน ป้องกันอาการ Layout Thrashing
- **Intersection Observer:** ใช้ Vanilla JS ตรวจจับการเลื่อนจอ เพื่อสั่งเล่นวิดีโอและทำแอนิเมชันเมื่อผู้ใช้เลื่อนมาถึงเท่านั้น เพื่อประหยัดหน่วยความจำ
- **Lenis Smooth Scroll:** ติดตั้ง Lenis Library เพื่อสร้างประสบการณ์การเลื่อนจอ (Scroll Momentum) ที่นุ่มนวลและดูพรีเมียม

**3. Fluid Responsive Design**
- หลีกเลี่ยงการล็อกค่าพิกเซลตายตัว (Fixed `px`) โดยใช้ `rem`, `vw/vh` และฟังก์ชัน `clamp()` เพื่อให้ขนาดฟอนต์และระยะห่างยืดหยุ่นตามหน้าจอ (Fluid Typography & Spacing) สวยงามเป๊ะทุกอุปกรณ์

## 🛠️ How to Run (การรันโปรเจกต์)
1. Clone repository นี้:
   ```bash
   git clone https://github.com/ChuthamatDev/web-plaimanas-test.git
